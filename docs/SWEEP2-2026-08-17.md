# Second sweep — 17 August 2026

46 agents. 112 candidates, 36 verified, **33 confirmed**.

## Progress, 17 August 2026

**8 of 33 cleared** in this pass. Fixed here:

- The Mistmoore backstab figures (findings 19, 20) — see `_build/backstab.py`.
  CLAUDE.md and three pages carried counts and a damage range that
  `assets/measured.json` does not hold. All four now derive from the dataset.
- Splitpaw's `zem_pct` (10, 14) — a stored 170 against a computed 171, and a
  "highest of the set" claim that Kedge Keep's 185% disproves. `gate.py` check
  3b now refuses any zem_pct that disagrees with its own ZEM.
- Crushbone's respawn (12) — a flat 9:00 where the ledger records a `<=9:00`
  pre-patch ceiling.
- Kedge Keep's continent (11) — Dagnor's Cauldron is on Faydwer, not Odus.

**Still open: 25 confirmed findings**, listed below. The largest cluster is the
race-unlock tracker (findings 1–7), which no session has audited against
`assets/faction-data.json` since the extractor was rewritten.


## 1. `tools/combo-calculator.html and tools/race-unlocks.html (identical) — ` — contradictory

> cost:"Six or seven raid-gated tests" … and … d:`Six or seven tests, end-game gated. What you fall back on once the free token is spent.`

**Why:** The site's own Plane of Sky dataset, assets/sky.json, gives the test count per class and it is not six or seven for four of the sixteen classes the tool offers: Paladin 4 ('Test of Spirit', 'Test of Sacrifice', 'Test of Love', 'Test of Compassion'), Beastlord 5, Magician 7, Shadow Knight 7. The string is rendered for whichever class the user picked, so a user asking about Paladin is told a hardcoded figure that the site's own tracker contradicts on the next page. Both classes are live options he

**Fix:** In _build/build21.py, replace the hardcoded sentence at lines 116-118 and derive the figures from MOTES so they cannot drift from assets/motes.json.

Replace:
  "To an <em>item</em> it is a loss: two rank 5s are <strong>10</strong> item XP and the rank 6 they become is <strong>6</strong>. <strong>Four item XP gone, every time.</strong>"

With the r

## 2. `tools/combo-calculator.html and tools/race-unlocks.html — Combo calcul` — false

> Granted the first time you reach level 50. Spend it on <b>[class]</b>. This is the only route that does not cost a full raid quest line or a reroll.

**Why:** It is not the only such route. A Primary Class Unlock Token is purchasable at 1,500 IR per the Producer's Letter of 8 July 2026, printed and badged T1 at public/learn/deity.html, and public/tools/plane-of-sky.html states plainly: 'A Primary Class Unlock Token skips a whole line, and you get one free the first time you reach level 50.' So buying a token is a third route that costs neither a raid quest line nor a reroll, and the calculator's own sibling tool describes it. The word 'only' makes thi

**Fix:** In _build/build13.py, ENTRIES[5]['legends'], change the lead sentence to "<strong>The lockout is per boss and per difficulty.</strong>" and let the rest of the paragraph carry the news, since the per-boss half is what the 23 June patch note already established and only the per-difficulty half is new. Keep the existing note hedging the per-difficult

## 3. `tools/combo-calculator.html and tools/race-unlocks.html — 'My route' t` — contradictory

> most quests take a single item type, so they hand in eight at a time

**Why:** The page's own STEPS table says four, never eight, in five separate places: batwings 'Four wings per turn-in, so 1600 wings is 400 cycles'; mead 'Four mead per cycle'; lionmeat 'Four shipments per turn-in'; iksarore '+5 for 4 Metal Bits'; phosph 'per 2 powder'. Nothing anywhere in the dataset hands in eight of anything, and 'eight at a time' halves the reader's estimate of how many trade-window cycles the route actually costs. Nothing else on the site states a batch size.

**Fix:** Edit _build/source/eql-race-unlocks.html (NOT the generated public/tools/*.html — _build/build3.py lines 365-375 injects this one source into both pages and would overwrite a direct edit), then run ./build.sh, node scripts/toolsmoke.js and python3 scripts/check.py.

1. Line 186, replace the final sentence of the Race Unlock Tokens bullet with the t

## 4. `tools/combo-calculator.html and tools/race-unlocks.html — Race unlocks` — contradictory

> Every race except Kerran unlocks by reaching <strong>+2000 personal faction</strong> with a set of factions — set against HUMF's own figures: tonics110 qty 110, 'Coalition of Tradefolk +10 · … · Knights of Truth +10'; intercept qty 42, 'Freeport Militia +25 … per note'

**Why:** Human (Freeport) is the only race in the roster of sixteen whose own quantities do not reach the threshold the page states. Multiplying quantity by per-item gain: Coalition of Tradefolk 110 × 10 = 1,100; Knights of Truth 110 × 10 = 1,100; Freeport Militia 42 × 25 = 1,050 — all roughly half of +2000. I checked all fifteen other races the same way and every one lands on 2000 or above exactly (Dark Elf 400 × 5, High Elf 1600 wings ÷ 4 × 5, Erudite 286 molars × 7 = 2,002, Iksar 180 × 10 + 160 ÷ 4 × 

**Fix:** Replace the invented range with what the parse holds, separated by mob type as CLAUDE.md requires, and name the session it comes from. The reporter's wording checks out against the largest session and can be used as written: "An initiate familiar, melee averaging 8.8 to a maximum of 39, backstab averaging 62.8 to a maximum of 143; A pledge familiar

## 5. `tools/character.html — 'Race unlocks' card; emitted by _build/build15.` — contradictory

> <p class="sub">Read from the race unlock tracker on this browser.</p> … <b id="r-done">0</b><span>Unlocked</span> … <b id="r-left">16</b><span>Not started</span>

**Why:** The sheet and the tracker disagree about the same saved state. raceProgress() in build15.py reads only bit 0, the 'Mark unlocked' flag, and skips the faction bits entirely (`i+=2+RACES[r].facs`). The tracker's isUnlocked() also returns true when every faction box for a race is ticked, and for Half Elf when Wood Elf or Human (Qeynos) is unlocked. So a user who ticks Dark Elf's three faction boxes sees 'Races unlocked 1 / 16' and the status 'available' in the tracker, then 'Unlocked 0' and 'Not st

**Fix:** Replace the warn note body in _build/build21.py:147-151 with:

"One claim does not fit the patch notes." The guide shows ten mote ranks. The 21 June 2026 note limits what the conversion construct will accept to ranks 1-8 — which puts rank 9 within reach of a conversion, and rank 10 outside it, since a rank 10 would need two rank 9s accepted. "Eithe

## 6. `tools/combo-calculator.html and tools/race-unlocks.html — footer, 'Whe` — unsourced

> every quantity, NPC, zone and per-turn-in faction figure below traces to it [Alanna's Race Unlock Guide on eqlwiki.com, revision 166686, last edited 5 August 2026]

**Why:** CLAUDE.md §2 places 'Alanna's Race Unlock Guide' at tier 3 by name, and docs/SOURCES.md lists it as an eqlwiki user guide. CLAUDE.md then requires: 'Tiers 3, 4 and 5 carry a visible badge wherever the claim appears … A T3 claim printed bare is a bug, and a worse one than a broken link.' Grepping both files returns zero occurrences of `class="tier`. So the entire dataset — 24 grind steps, 16 races, every faction figure and hour estimate — prints bare, as if tier 2. The deliberate exception in CLA

**Fix:** Recompute from assets/measured.json and state the scope, in one commit covering every carrier.

_build/build13.py ENTRIES[7] (entry begins line 249): line 258 note, change "backstabbed 39 times between them" to "backstabbed 113 times between them"; line 263 evidence, change to "An initiate familiar 51 backstabs and a pledge familiar 62, 113 between

## 7. `tools/combo-calculator.html and tools/race-unlocks.html — STEPS.mead; ` — contradictory

> gain:"Corrupt Qeynos Guards +5 · Circle of Unseen Hands +5 · Kane Bayle +5 — per 4 mead" … note:"Trades guards and merchants away for corrupt guards, but Legends' +5 minimum gain makes it come out ahead."

**Why:** The note asserts faction losses that the step's own data field does not record, and the losses land on two of the three factions the same race needs. Human (Qeynos) requires Merchants of Qeynos, Guards of Qeynos and Corrupt Qeynos Guards, and Merchants of Qeynos and Guards of Qeynos come only from the brandy step. The gain string lists three positives and no negative. Four other steps in the same table do record their losses — kobolds 'Clan Kobolk −1', molars 'Heretics −1', grublocker 'Broken Sk

**Fix:** Edit _build/source/eql-race-unlocks.html line 475 (never the two public/tools/ files — they are generated by _build/build3.py), then run ./build.sh, node scripts/toolsmoke.js and python3 scripts/check.py.

Do not substitute "four" for "eight": the dataset carries three different batch sizes (4 for batwings/mead/lionmeat/Metal Bits, 2 for phosph, 1 

## 8. `tools/combo-calculator.html and tools/race-unlocks.html — injected nav` — stale

> Sourced &amp; dated &middot; updated daily

**Why:** docs/AUTOMATION.md describes a twice-daily check of the wiki API that opens a pull request and stops — 'The gate is the merge. Nothing reaches the site without you pressing a button' — not a daily update. And this tool's data has had no content change since the 7 August 2026 contrast pass: git log on _build/source/eql-race-unlocks.html shows only 2026-08-07 (contrast) and 2026-08-16 (design system), against source revisions the footer dates 5 August 2026 and 19 July 2026. The banner promises a f

**Fix:** In _build/build21.py, badge the costbar block. Prefer the trailing paragraph over the heading, to avoid colliding with the bar's own "T1".."T10" upgrade-tier labels: at line 101-103 change the closing sentence of `<p class="lede">The cost curve doubles too, ... in a way spells never do.</p>` to end `...in a way spells never do. <span class="tier t3

## 9. `public/dungeons/crushbone.html (§01 "Why you come here"; also the subt` — false

> Nine-minute respawns, the fastest here.

**Why:** Four zones surveyed on this same site respawn faster. Befallen's strip prints "Respawn 4:30 · fastest in the bracket", Najena 4:50, The Warrens ≤6:40, The Hole 7:48 — all in assets/zones-index.json and all rendered on their own pages. Crushbone's own ledger entry is "respawn": "<=9:00", i.e. a ceiling, not even a rate. The claim is false under every reading of "here": it is not the fastest on the site, and it is not the fastest among the early dungeons either (The Warrens is 4–25 at ≤6:40, Befal

**Fix:** Port the tracker's three unlock paths into `raceProgress()` in `_build/build15.py` (lines 215–227): a race counts as unlocked if its `owned` bit is set, OR it has at least one faction and every faction bit is set, OR it is `derived` and any of its parents is unlocked. Decode all bits into an array first (mirroring the tracker's `applyBits`) rather 

## 10. `public/dungeons/splitpaw.html (header strip, ZEM cell)` — false

> 170% — highest of the set

**Why:** Kedge Keep, plate 13, publishes "ZEM 139 · 185% — highest we have recorded" in the identical strip cell. 185% > 170%, so Splitpaw is not the highest of anything. The page also contradicts itself two sections later: §01 says "The joint highest experience modifier here", which is not the same claim as "highest". The string is a leftover from when the site surveyed ten zones; it now surveys thirteen. Source: _build/source/splitpaw.html line 88 and assets/zones-index.json ("zem_pct": 170); page emit

**Fix:** Take the second option: correct the sentence in _build/build13.py, ENTRIES[8]['legends'] (lines 284-285), to describe what the site actually does. Replace "and the site prints them struck through rather than deleting them" with something like "and the site strikes through the placeholder sentences while the inherited percentages still print bare; t

## 11. `public/dungeons/kedgekeep.html (eyebrow line above the H1)` — false

> Dungeon Survey Plate 13 · through Dagnor&rsquo;s Cauldron, Odus

**Why:** Dagnor's Cauldron is on Faydwer, not Odus. The site says so itself: public/dungeons/crushbone.html §06 states "Circle of Butcherblock (Druid) or the Butcherblock translocator both land on Faydwer", and Butcherblock Mountains is Dagnor's Cauldron's neighbour. Odus on this site is Paineel — plates 08 (The Hole) and 09 (The Warrens) both carry the eyebrow "Paineel, Odus". No other file in the repo associates Dagnor's Cauldron with Odus; the only three Dagnor's mentions are all on this one page. Sou

**Fix:** In _build/source/eql-race-unlocks.html, stop hardcoding the count in both places, then run ./build.sh so both generated pages update.

Preferred: have _build/build3.py substitute the per-class test counts from assets/sky.json into the tool at build time (the same way _build/build8.py substitutes const SKY into plane-of-sky.html), keyed by the class

## 12. `public/dungeons/crushbone.html (header strip) — printed bare, with no ` — stale

> <div class="cell"><dt>Respawn</dt><dd>9:00</dd></div>

**Why:** The same page's own generated meta description reads "respawn at most 9:00", and assets/zones-index.json records "respawn": "<=9:00" with a respawn_note that says in terms: "Pre-dates the 28 Jul patch, which reduced Orc Scoutsman and Orc Warden timers substantially, so treat as a pre-patch ceiling. Marked as a ceiling on 11 Aug 2026: this note already called it one and the figure printed bare." The page's own roster then twice says "Respawn substantially reduced 28 July" (Orc Warden, Orc Scoutsm

**Fix:** Fix in _build/source/eql-race-unlocks.html and rebuild — do NOT edit public/tools/*.html, which _build/build3.py overwrites.

Do not adopt the reporter's suggested "200 tonics, 80 notes". The arithmetic lands on 2,000 correctly, but those digits were reverse-engineered from the threshold, not read from a source, and typing them would breach the har

## 13. `public/dungeons/splitpaw.html (§02 "Know where you are", last rows of ` — contradictory

> Prison cells</td><td>Map point 6

**Why:** Map point 6 is not the prison. The page's own named roster gives the prison mobs numbers 3 and 4 — "3 · Brother Hayle · 382, 329 · Human cleric held in the prison" and "4 · Brother Gruff · 357, 246" — and gives 6 to "A Lteth Val Scribe · 374, −26 · Back of a passage after dropping off the first rock bridge and heading east". The numbering these come from is the archived plate legend in assets/archive-plates.json, which reads "3 Brother Hayle (prison)", "4 Brother Gruff", "6 Lteth Val Scribe". So

**Fix:** Edit _build/source/eql-race-unlocks.html line 183 — the single editable original; _build/build3.py:365 and :370 copy it to both public/tools/race-unlocks.html and public/tools/combo-calculator.html, so one edit fixes both pages. Do not edit the public/ files directly (CLAUDE.md section 3).

Cheapest correct fix: add <span class="tier t3">T3</span> 

## 14. `public/dungeons/splitpaw.html (header strip ZEM cell) — the percentage` — contradictory

> 170%

**Why:** The Hole and The Warrens carry the identical ZEM of 128 and both print "171%". Every other percentage on the site is ZEM/75 rounded: 119→159, 97→129, 86→115, 82.5→110, 139→185. 128/75 = 170.67, which rounds to 171, not 170. So Splitpaw's stored zem_pct of 170 in assets/zones-index.json is simply the odd one out, and the same number now reads two ways depending on which survey you open.

**Fix:** In _build/build3.py line 36, drop the cadence clause: `<span class="ns-tag">Sourced &amp; dated</span>`. Do NOT substitute "Sources checked twice daily" — see reasoning, that is currently false too. If a freshness signal is wanted, stamp the source file's own last content-change date, which the build already knows. Note the tag is injected onto 21 

## 15. `public/dungeons/kedgekeep.html` — contradictory

> Mob levels 32–50 (header strip) against "Phinigel Autropos … Lvl 53" (§06 named roster)

**Why:** The strip publishes a mob level band of 32–50 and the roster immediately below lists a mob at 53, three levels above the stated ceiling, with no note reconciling the two. The generated meta description repeats the band bare: "Levels 32-50, respawn not recorded." Splitpaw handles the same situation correctly — its ledger levels field ends "; named exceed", which propagates into its meta description. Kedge's levels field in assets/zones-index.json is a flat "32-50" with no such qualifier. Source: 

**Fix:** In `_build/source/befallen.html` line 97, replace the second `<li>` with:

"<b>4:30 respawns, the fastest figure we hold.</b> Crushbone's 9:00 and Blackburrow's 22:00 are both unfirm — Crushbone is a pre-patch ceiling, and whether Blackburrow's 22:00 survived the 28 July patch is unresolved. The real gap may be smaller than those numbers suggest an

## 16. `public/dungeons/splitpaw.html (§05 named roster, The Ishva Mal row)` — contradictory

> Spawns roughly every 36 hours on a 28-minute respawn

**Why:** The page publishes a zone respawn of 22:00 in its header strip, its generated meta description says "respawn at most 22:00", and assets/zones-index.json records "<=22:00" — an explicit maximum, lowered by the 28 July patch. A 28-minute placeholder cycle sits six minutes above that stated maximum, printed bare with no note that it is an exception or a pre-patch figure. The neighbouring Tesch Val Deval`Nmak row ("PH and named both on a 20-minute timer") sits under the ceiling, so the roster is not

**Fix:** Fix the parser, not just the symptom. In _build/logstats.py, BY_SPELL (line 166) accepts any trailing "by X." clause on the line after "You are stunned!", which is why "A lava duct crawler has been awakened by Avenrae." and "…is pierced by Avenrae's thorns for 17 points of non-melee damage." became stun keys. Require the attribution line to be a da

## 17. `public/dungeons/splitpaw.html (§03 "Reading a gnoll", lede)` — unsourced

> Splitpaw shares something with only one other zone in Norrath: the gnolls have a full class hierarchy, and their names announce what they will do to you before you pull them.

**Why:** The page never names the other zone and cites nothing for the claim. The only other gnoll zone surveyed on this site is Blackburrow (plate 5), and its eighteen-name roster contains no Tesch, Lteth, Nisch or Rosch prefix and no Mas/Mal/Val power rank — its names are "Sabertooth Overseer", "Lord Elgnub", "Splitpaw Sharpshooter" and so on. So nothing on the site supports "only one other zone", and the one candidate the site actually documents does not fit. Source: _build/source/splitpaw.html line 1

**Fix:** Fix the lede, not the table - the table's data already propagates to three item pages and index-data.json, while the rule appears nowhere else.

In _build/source/befallen.html line 106, replace:

  <p class="lede">Three locked doors, three shadowknights. Every shadowknight drops its own key <em>and every key below it in the chain</em>, so the deepe

## 18. `learn/still-true.html (entry q9, "Do named mobs still have placeholder` — false

> For the ten that are named, every inherited spawn percentage is historical, and the site prints them struck through rather than deleting them.

**Why:** The site does not do this. Across the ten placeholder-removed surveys there are 112 spawn percentages inside the roster's "Spawn & notes" cells (befallen 10, blackburrow 1, crushbone 7, lowerguk 21, mistmoore 18, nagafenslair 14, najena 8, splitpaw 6, thehole 10, warrens 17) and exactly two are struck through: lowerguk's "Placeholder is a froglok guk shaman at 3%" and najena's Drelzna "20%", the latter via class="disputed" for a different reason. Everything else prints bare - lowerguk "25%. Drow

**Fix:** Widen PH_CLAIM in _build/build3.py (lines 206-211) to accept the abbreviation, KEEPING the two negative lookbehinds — the reporter's proposed pattern drops them, which would start striking the deliberately-corrected form ("there are no placeholders here", "not a placeholder") and regress Drelzna's row. Use:

    PH_CLAIM = re.compile(
        r'(?<

## 19. `learn/still-true.html (entry q8, "Does raising the difficulty just mak` — stale

> two ordinary trash types backstabbed 39 times between them ... An initiate familiar 22 backstabs and a pledge familiar 17

**Why:** assets/measured.json, the Tier M parse the line cites, does not contain those numbers anywhere. Across the seven 8 Aug 2026 Castle Mistmoore sessions it records A pledge familiar 62 backstabs and An initiate familiar 51 - 113 between them. Restricted to Avenrae's four sessions (which is the scope the Mistmoore survey itself publishes: "4 sessions, 08 Aug 2026, 11:08-18:14. Measured from Avenrae's log") it is 57 and 44, or 101. No single session and no combination of sessions produces 22 and 17; 

**Fix:** Edit _build/source/blackburrow.html (never the generated page) and rerun ./build.sh, then scripts/check.py.

Drop the bare single-class assertions from the three Notes cells and replace each with what the log supports, keeping the position detail that is genuinely useful:

- Line 126, Sabertooth Overseer: replace "A berserker gnoll. Usually the fir

## 20. `learn/still-true.html (entry q8, Tier M evidence line). Emitted by _bu` — false

> Melee 1&ndash;38, backstab 100&ndash;143.

**Why:** Both ends are contradicted by assets/measured.json. Backstab cannot start at 100: the per-session backstab averages recorded for those two mob types are 57.9, 62.8, 88.4, 68.8, 62.2 and 38.8 - a set of hits averaging 38.8 with a maximum of 72 has no member at 100. Nor does it stop at 143: the recorded backstab maxima include 152, 159 and 168. The melee figure is wrong too - A pledge familiar's melee maximum is 38 but An initiate familiar's is 39, so the stated ceiling excludes one of the two typ

**Fix:** In _build/source/blackburrow.html line 214 (NOT the generated public/ copy), cut the second sentence and state the gap in the site's own convention. Replace:

  <li><strong>Freeport ↔ Qeynos translocator.</strong> Legends added a direct route between the two cities. Translocators are <strong>free until you reach level 20 in any class</strong>, and 

## 21. `learn/still-true.html (entry q6, "Does killing a raid boss lock you ou` — contradictory

> The lockout is per difficulty, not per boss.

**Why:** The entry's own tier 1 evidence says the opposite of the "not per boss" half. The 23 June 2026 note it quotes reads "you will only receive 1 piece of loot per named raid creature that has its own lockout" - a lockout attached to a named raid creature is per boss by definition, and docs/PLANES.md records the same wording. The entry's next sentence relies on that too: "so the same boss can be run once per tier" only means anything if the lockout is tracked per boss as well as per tier. The player 

**Fix:** Edit _build/source/blackburrow.html, not the generated page, then run ./build.sh.

1. Line 89, strip cell. Do not use "&le;" — on the five zones that carry it, &le; asserts a known pre-patch ceiling, and Blackburrow's state is weaker than that. Mark it as unresolved instead, e.g. <div class="cell"><dt>Respawn</dt><dd>22:00<small>unresolved &mdash; 

## 22. `learn/motes.html ("The rule that saves you the most", the danger note)` — false

> To an item it is a loss: two rank 5s are 10 item XP and the rank 6 they become is 6. Four item XP gone, every time.

**Why:** Four is only the rank 5 case. assets/motes.json sets item_xp equal to the rank, so converting two rank r motes into one rank r+1 loses 2r - (r+1) = r - 1 item XP. By the page's own table that is 1 XP at rank 2, 2 at rank 3, 4 at rank 5, and 8 at rank 9 - and 0 at rank 1, where converting costs nothing. "Every time" states a constant where the page's own data gives a rising curve, and it understates the loss by half at the top of the scale, which is where the decision actually matters.

**Fix:** In _build/logstats.py, tighten the stun attribution at lines 401-412. (1) Ignore candidate lines that are damage-shield lines — anything whose captured tail matches "for N points of non-melee damage", and anything beginning "YOUR " or "<known player>'s " — since the character's own and a party member's damage shield hitting a mob cannot stun the ch

## 23. `learn/motes.html ("Where this came from, and one thing that does not f` — false

> The 21 June 2026 note says the conversion construct "can only accept ranks 1-8". Either it was extended since, or ranks 9 and 10 exist but cannot be reached by converting. We do not know which, and one attempt to convert two rank 8 motes would answer it.

**Why:** Both halves fail against the note the page itself quotes. docs/PLANES.md records it as: a construct "can condense your motes into stronger versions, at a two-to-one rate! NOTE: For the moment, he can only accept ranks 1-8." Accepting rank 8 as input is exactly what produces a rank 9, so rank 9 IS reachable by converting and the stated alternative is wrong about it - only rank 10 is out of reach, since that would need rank 9 accepted. And the proposed test cannot discriminate: converting two rank

**Fix:** Two parts. Do not edit public/dungeons/ — it is generated.

1. Authored text, _build/source/befallen.html line 137. Delete the derived advice and the badge, keep the struck figure so the record stays checkable, and state the measurement:

`<tr><td class="lv">18</td><td class="nmob">Gynok Moltor</td><td class="loc">−80, −45</td><td class="lv">24</td

## 24. `learn/still-true.html (entry q9, evidence list). Emitted by _build/bui` — unsourced

> T1 — Developer patch notes — "Removed placeholders from and lowered maximum respawn times in several dungeons: The Hole, Nagafen's Lair, Lower Guk, Lair of the Splitpaw, The Warrens, Castle Mistmoore, Upper Guk, Crushbone, Befallen, Blackburrow, Najena."

**Why:** The citation carries no date, on a page whose own lede promises "one entry per piece of inherited advice, each with its evidence, its date" and whose footer promises "the date that source was read". Every other tier 1 line on the page is dated ("Patch notes, 23 June 2026", "Patch notes, 28 July 2026"); this one is not, and it is the sole source for a verdict the entry marks Settled across eleven zones. The date is not recorded anywhere else either - assets/zones-index.json repeats the same quote

**Fix:** In _build/source/thehole.html line 89, replace

    <div class="cell"><dt>Respawn</dt><dd>&le;21:30<small>max lowered 28 Jul</small></dd></div>

with the ledger value, and drop the false "max lowered 28 Jul" gloss (the change was a 14 July wiki correction, not a patch ceiling). Because eqlwiki rev 157133 is dated 14 Jul 2026 - pre-launch, and so ti

## 25. `learn/motes.html. Emitted by _build/build21.py:99-100 from assets/mote` — unsourced

> The upgrade-cost bar: T1 1 · T2 2 · T3 4 · T4 8 · T5 16 · T6 32 · T7 64 · T8 128 · T9 256 · T10 512, under the heading "What each upgrade tier costs".

**Why:** It prints bare, with no tier badge. Every figure on this page comes from the same T3 infographic - motes.json's own comment says so, and build21.py's docstring says "it is published as T3 throughout" - and every other block on the page carries the badge: the ranks table ("Table T3"), the eligibility note, the Void Gems paragraph. The costbar is the one exception, and the badge above it is explicitly scoped to "Table", not to what follows the next heading. CLAUDE.md's rule is that tiers 1 and 2 p

**Fix:** Change 4:27 to 4:30 in assets/zone-provenance.json, key "warrens", note index 3 (the was-status block) - the sentence "though still slower than Befallen's 4:27". That file is the source data _build/build2.py reads at line 28 and renders into the zprov blocks at lines 52-55, so rebuild with ./build.sh afterwards. Leave the two other 4:27 mentions on

## 26. `dungeons/thehole.html (line 191; authored in _build/source/thehole.htm` — stale

> <dt>Respawn</dt><dd>&le;21:30<small>max lowered 28 Jul</small></dd>

**Why:** The page contradicts itself in the <head>. Its own <meta name="description"> (line 5) and og:/twitter:description (line 84) both read "respawn 7:48" — build3.py lines 286-295 generate them from assets/zones-index.json, which has "respawn": "7:48" for thehole. That entry's respawn_note retracts 21:30 by name: "eqlwiki rev 157133, 14 Jul 2026, editor Realpoor, 'updated respawn timer', replacing 21:30. Our 21:30 was captured on 30 Jul from a figure the wiki had already superseded two weeks earlier 

**Fix:** Delete "— was 160" from _build/source/befallen.html line 88, so the cell reads `<dd>119<small>159%</small></dd>`, matching Blackburrow and Crushbone at the same value, then run ./build.sh so build3.py re-emits public/dungeons/befallen.html. Restore it only if a source is found: if the 23 June rebalance really lowered Befallen, that belongs in asset

## 27. `dungeons/thehole.html (subtitle line 186, section 01 bullet line 205, ` — false

> The Ruins of Old Paineel — highest zone experience modifier in the game … The highest zone experience modifier in the game. ZEM 128, across a 39–56 band.

**Why:** Kedge Keep is higher. assets/zones-index.json gives kedgekeep zem 139 / zem_pct 185 against thehole zem 128 / zem_pct 171, and dungeons/kedgekeep.html prints "ZEM 139<small>185% — highest we have recorded</small>" in its strip and "ZEM 139, or 185%. The highest figure on this site" in its section 01. sources.html carries the same in its change log: "Kedge Keep surveyed — Survey 13, and the highest experience rate we have recorded — ZEM 139, or 185%". The Hole is also only joint-second: dungeons/

**Fix:** Edit `_build/source/befallen.html` line 133 and run `./build.sh` — do not touch the generated pages, and do not touch the archive, which does not carry this line.

Replace the row's notes cell with:

`Gnome necromancer. <strong>Maximum melee hit not recorded.</strong> Reported to keep a pet and to self-buff on spawn with Leatherskin and a haunting 

## 28. `dungeons/thehole.html (roster row line 218 and loot note line 283; aut` — false

> Bottom of the pit. Death touches. Sees through invisibility. Casts Ice Comet. Raid instance only. … Four of the five best items are behind the raid instance. Master Yael has been raid-instance-only since 11 June, so Earthshaker, Brell's Girdle, the Loam Encrusted Robe and Serpent's Tooth are not ope

**Why:** Our own tier-M record falsifies it. assets/raids-measured.json holds seven Master Yael kills, on 10 and 11 Aug 2026 — two months after 11 June — and every one carries "group_instance": true with zone "The Ruins of Old Paineel - Group" through "- Group 4 (Refined)", at D0, D1 ×2, D2 ×2, D3 and D4. assets/measured.json records the same kills in "The Ruins of Old Paineel - Group" sessions. CLAUDE.md section 9 states it outright: "Master Yael was killed at all five tiers in one session in the group 

**Fix:** Fix the authored rows in `_build/source/blackburrow.html` lines 188 and 189, then rebuild. Do not silently append "Tranixx Darkpaw" to a read-sourced dropper list — that blurs tiers, and the site's own convention (item pages) keeps read and measured droppers in separate labelled blocks. Add him with a tier M marker and the dates, e.g. `Gnoll comman

## 29. `dungeons/thehole.html (Dangers, line 381; authored in _build/source/th` — false

> Backstabs land for 348. Elemental capturers hit that figure; the weaker deceivers run around 248 with a maximum near 300.

**Why:** Contradicted by the measured data behind this page's own Measured in play block. Aggregating every Hole session in assets/measured.json: an elemental capturer backstabbed 11 times for an average of 149.9 and a maximum of 359; an elemental deceiver backstabbed 32 times for an average of 151.6 and a maximum of 365. So the deceiver's maximum is 365, not "near 300"; its typical backstab is 152, not "around 248"; and it is not "weaker" than the capturer — its average and maximum are both slightly hig

**Fix:** Edit _build/source/thehole.html line 84 (subtitle) and line 103 (section 01 bullet), then rebuild — the three <head> descriptions and the search index regenerate from the subtitle.

Line 84 subtitle: "The Ruins of Old Paineel — joint second experience modifier on the site, behind Kedge Keep"

Line 103 bullet: "<li><b>ZEM 128, or 171%.</b> Joint hig

## 30. `dungeons/thehole.html (Measured in play, line 475; generated by _build` — contradictory

> 205 kills across 15 kinds of mob … Plus 19 ordinary mob types over 19,973 swings, landing 46%.

**Why:** The two counts in the same generated block cannot both be right: 15 kinds of mob against 7 itemised named rows plus 19 unitemised trash types, which is 26. The data says 15. The six merged sessions (Avenrae, D1 Awakened, 10–13 Aug 2026, 205 kills — all three figures check out) hold 26 keys in their `mobs` maps because assets/measured.json splits every mob type into a capitalised key carrying swings and damage and a lowercase key carrying loot: 'An elemental warrior' with 4,372 swings sits beside

**Fix:** Backstabs are the hidden damage here, and the deceiver is the one to watch. Measured across the six D1 sessions below: an elemental deceiver averaged 155 from behind against 40 in melee, maximum 365 over 31 backstabs; an elemental capturer averaged 67, maximum 105, over 4. A muck covered elemental hit 381 at its worst. At D0 the ordering reverses —

## 31. `dungeons/thehole.html (Measured in play named table, line 475; generat` — contradictory

> Muck covered elemental … 246 … 46% … 73.4 avg · 151 max … backstab 362 avg · 381 max, seen 2× … [drops] — [and, two rows later] muck covered elemental … no casts logged … — … — … not measured … Fire Opal

**Why:** One mob type published as two rows, asserting two things that are both false: that the mob which swung 246 times dropped nothing, and that the mob which dropped a Fire Opal was "not measured". assets/measured.json's `kinds` arrays list only 'muck covered elemental' — one type. Its `mobs` map splits it: 'Muck covered elemental' holds 210 swings on 10 Aug and 36 on 11 Aug (246 total, matching the page) with empty loot, while 'muck covered elemental' holds 0 swings and loot {"Fire Opal": 1}. Same s

**Fix:** Merge the mobs map case-insensitively. Preferred fix is upstream in _build/logstats.py, so assets/measured.json, public/data/*.vN.json and every consumer agree on one key per mob type; the minimal fix is _build/build9.py line 213, changing mobs.setdefault(name, ...) to key on name.casefold() while keeping the first-seen capitalised form for display

## 32. `dungeons/warrens.html (banner line 190, roster row line 207; banner in` — contradictory

> Named spawn every cycle. No placeholders here. … [twelve rows later] Packmaster Dledsh … 100% on a 16-minute timer, but the placeholder is a frenzied rat and the observed rate was two spawns in eighty-five kills.

**Why:** The banner asserts there are no placeholders in this zone; the roster then names one in the present tense and bold. The page proves the sentence was meant to be retracted, because the other two placeholder claims on it were: Grodl Ripclaw (line 203) and Warlord Drrig (line 204) both ship wrapped in <span class="ph-old" title="Historical. The 28 July 2026 patch note removed placeholders from this zone."> with a "was" tag, and Packmaster Dledsh does not. The cause is build3.py's PH_CLAIM regex at 

**Fix:** Correct the authored source, `_build/source/thehole.html`, then rebuild so the string propagates.

Line 113, roster: keep the measured and unrefuted claims, replace only the false clause — "Bottom of the pit. **Death touches. Sees through invisibility. Casts Ice Comet.** Reachable in the group instance: killed there at all five tiers, D0-D4, on 10-

## 33. `dungeons/thehole.html (roster row line 218; authored in _build/source/` — unsourced

> Death touches. [Master Yael, with a red "death touch" badge]

**Why:** Printed bold and badged with no source, and unsupported by the only Legends-era evidence the site holds on this mob. assets/raids-measured.json records seven Master Yael kills at all five difficulty tiers on 10–11 Aug 2026, with the full spell list for each; none contains a death-touch or harm-touch effect. The parser demonstrably captures such effects — "Harm Touch" appears in Cazic-Thule's spell list in the same file, and this page's own roster records Commander Yarik at "Harm Touches for 511"

**Fix:** Fold mob keys case-insensitively so the pair renders as one row: "Muck covered elemental", 246 swings, 46% landed, 73.4 avg · 151 max, backstab 362 avg · 381 max seen 2×, Dropped: Fire Opal.

Best fixed at source in _build/logstats.py, where mob identity is established — canonicalise the name once (e.g. keep the first-seen spelling per casefolded k
